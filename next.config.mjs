/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/ColmenaUrbana',
  assetPrefix: '/ColmenaUrbana',
};

export default nextConfig;